{
  "name": "@lettercast/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wxt build",
    "dev": "wxt",
    "postinstall": "wxt prepare",
    "test": "vitest run",
    "typecheck": "wxt prepare && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "wxt": "0.21.4"
  }
}

