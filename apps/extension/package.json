{
  "name": "@lettercast/extension",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wxt build",
    "dev": "wxt",
    "postinstall": "wxt prepare",
    "test": "vitest run && node ../../scripts/verify-service-worker-boundary.mjs",
    "typecheck": "wxt prepare && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lettercast/contracts": "workspace:*"
  },
  "devDependencies": {
    "wxt": "0.21.4"
  }
}
